<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.project.william.flashcards" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MenuScreen"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ViewDecks"
            android:label="FlashCards"
            ></activity>

       <activity android:name=".DeckCreations"
           android:label="FlashCards"
           ></activity>

        <activity android:name=".FileBrowserCreation"
            android:label="FlashCards"
            ></activity>

        <activity android:name=".HelpScreen"
            android:label="FlashCards"
            ></activity>
    </application>

</manifest>
